#include <stdio.h>
#include <stdlib.h>
#include <string.h>


/* . Pintura de cilindros. Faça uma solução para o usuário informar dados necessários para o sistema calcular e mostrar:
 o total necessário de latas de tinta e total a pagar pelas tintas. Cada lata de tinta tem 5 litros, e cada litro pinta 3m2.
 Os dados de entrada são: raio, altura e quantidade de cilindros, e valor da lata de tinta. */

int main(int argc, char *argv[]) {
	float r, h, vtinta;
	int qcilin, latatinta; 
	printf ("\n digite o valor do raio do cilindro \n");
	scanf ("%f", &r);
	printf ("\n digite o valor da altura do cilindro \n");
	scanf ("%f", &h);
	printf ("\n digite a quantidade de cilindros que deseja pintar \n ");
	scanf ("%d", &qcilin);
	printf ("quanto esta custando a lata de tinta");
	scanf ("%f", &vtinta);
	
	
	
	
	
	
	return 0;
}
